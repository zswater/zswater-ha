# Pins the Home Assistant version the suite runs against. The config-flow tests
# drive a real hass instance, so the version matters; pinned rather than floating
# so CI and local runs agree. Bump deliberately, then re-run.
pytest-homeassistant-custom-component==0.13.205
pytest
pytest-asyncio
aiohttp
